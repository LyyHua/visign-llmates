apiVersion: v1
kind: Service
metadata:
  name: visign-ai
  labels:
    app: visign-ai
spec:
  selector:
    app: visign-ai
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
